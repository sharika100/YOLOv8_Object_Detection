# 🧠 YOLOv8 Object Detection using Google Colab 🚀

This repository demonstrates how to perform **object detection** using the **Ultralytics YOLOv8** model in **Google Colab**.  
It uses a pre-trained YOLOv8 model (`yolov8n.pt`) to detect objects in a sample image or your own image.

---

## 🎯 Objective
- Load a pre-trained YOLOv8 model.
- Perform object detection on an image.
- Visualize and save the results.
- (Optional) Run real-time detection using a webcam.

---

